# Dependencies only. Source arrives via the bind mount in docker-compose.override.yml.
FROM node:24-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN --mount=type=cache,target=/root/.npm npm ci --no-audit --no-fund

EXPOSE 5173
CMD ["npm", "run", "dev"]
